# 精选安全面试题库

## 一、项目经历

**1. 选择简历中最能代表你能力的一个项目：你亲自负责了什么，哪些部分由团队其他成员或现有系统完成？**


**参考要点：** 能明确区分本人、团队和既有系统的工作，并给出代码、配置、流程或指标层面的证据。

**追问：** 指出一个由你亲自决定并承担结果的设计。

**2. 这个项目中最重要的一次技术或安全取舍是什么？如果重新做一次，你会改变什么？**


**参考要点：** 能说明可选方案、选择依据、代价和事后认识，而不只是复述最终实现。

**3. 你如何证明项目达到了预期效果？请说明使用过的测试、数据或其他证据。**


**参考要点：** 有基线、测试、覆盖率、误报率、修复时长、事故变化或其他可复核指标。

## 二、安全基础

**4. 服务端通常如何使用请求中的 `Content-Type`？为什么不能仅凭这个 Header 判断请求体的真实内容？**


**参考要点：** 服务端用它选择 JSON、表单、multipart、XML 等解析器，读取 `charset` 或 `boundary`，并决定是否返回 `415`。它只是客户端声明，可能与正文不一致；代理、WAF、框架和业务代码也可能采用不同解析规则。服务端还需限制允许类型、严格解析、校验解析结果，并独立验证上传内容。

**追问：** `Accept` 与 `Content-Type` 的区别是什么？解析差异可能如何影响参数处理或 WAF 判断？

**5. 一个服务收到 JWT 后，需要完成哪些检查才能决定是否信任它？用户退出登录或权限被收回后，已经签发的 JWT 应如何处理？**


**参考要点：** 校验允许的算法、可信密钥、签发者、受众、过期时间、Not Before、令牌用途和权限声明；考虑短有效期、刷新令牌、版本号、撤销列表与密钥轮换。Payload 通常只是编码，不是加密。

**6. 反射型、存储型和 DOM 型 XSS 的主要区别是什么？防护措施为什么需要考虑输出所处的上下文？**


**参考要点：** 反射型来自当前请求响应，存储型先持久化后触发，DOM 型关注客户端 source 到 sink 的数据流。文本、属性、URL 和 JavaScript 上下文需要不同编码或安全 API。默认转义、富文本清洗、CSP 和 HttpOnly 是不同层次的控制。

**追问：** 为什么过滤 `<script>`、使用前端框架或设置 HttpOnly 都不能单独消除 XSS？

**7. 参数化查询为什么能防御大多数 SQL 注入？哪些查询位置通常不能直接使用普通值占位符？**


**参考要点：** 参数化把查询结构与值分离。表名、列名、排序字段和排序方向等结构位置通常不能作为普通值参数，应使用固定映射、枚举或 ORM 列对象。使用 ORM 不自动等于安全。

**8. 哪些产品功能容易引入 SSRF？如果业务必须允许用户提供 URL，安全控制应该放在哪些环节？**


**参考要点：** 常见入口包括 URL 预览、图片抓取、Webhook、文件导入、代理和 Agent 工具。优先固定目标或使用业务白名单；规范化 URL；限制协议、端口和重定向；校验每次 DNS 解析的全部地址；阻止私网、回环、链路本地和其他保留地址；让实际连接使用已验证地址；设置出网策略、超时、大小限制和审计。

**9. 同源策略、CORS 与 CSRF 防护分别解决什么问题？它们之间有哪些容易混淆的边界？**


**参考要点：** 同源策略主要限制浏览器跨源读取；CORS 是服务端声明、浏览器执行的读取许可；CSRF 防护验证跨站状态变更请求的意图。CORS 不是服务端防火墙，也通常不能替代 CSRF Token、SameSite 或 Origin 校验。

**10. Linux 系统中，文件权限码 `600` 表示什么？如果它用于私钥或敏感配置文件，能够防止哪些访问，又不能解决哪些安全问题？**


**参考要点：** 三位八进制权限依次对应所有者、所属组和其他用户；`6` 是读权限 `4` 与写权限 `2` 之和，因此所有者可读写，所属组和其他用户没有权限，任何用户都没有执行权限。较好的回答还应指出，`600` 只约束传统 Unix 文件权限，不能防止 root 或具备相应 capability 的进程、文件所有者账号失陷、额外 ACL、错误复制或备份，以及应用自身读取后造成的数据泄露。

**11. 请写出一条 Linux 命令，递归查找 `/var/www/html` 目录下内容包含字符串 `password` 的文件，并且只输出匹配文件的路径。**


**参考要点：** 可以使用 `rg -l --fixed-strings 'password' /var/www/html`；需要包含隐藏文件并忽略规则文件时，可增加 `--hidden --no-ignore`。也可以使用 `grep -RIl -- 'password' /var/www/html`，其中 `-R` 表示递归查找、`-I` 跳过二进制文件、`-l` 只输出文件名。默认区分大小写，需要忽略大小写时增加 `-i`。

## 三、代码审计

代码题先让候选人说明外部输入、已有校验、危险操作和数据流，再讨论漏洞名称。

**12. 阅读下面的代码，判断是否存在 SQL 注入，并说明用户输入、校验逻辑和 SQL 执行点之间的数据流。**

```go
func ListAlerts(c *gin.Context, db *gorm.DB) error {
	direction := strings.ToLower(c.Query("direction"))
	if direction != "asc" && direction != "desc" {
		direction = "desc"
	}

	return db.
		Where("tenant_id = ?", c.GetString("tenantID")).
		Order("incident_time " + direction).
		Find(&[]Alert{}).Error
}
```


**参考要点：** 外部输入来自 `c.Query("direction")`，经过小写转换后与 `asc`、`desc` 做完全相等比较，不匹配时被替换为固定值 `desc`。虽然 `Order` 接收的是 SQL 结构片段，但到达执行点的值只可能是两个固定枚举，因此在当前代码和框架行为下不能构造 SQL 注入。审计时还应确认没有其他赋值路径，并查看 ORM 最终生成的 SQL，不能仅凭字符串拼接直接报漏洞。

**追问：** 如果代码改成 `Order(c.Query("sort") + " " + direction)`，安全结论会发生什么变化？请指出新的可控点和利用条件。

**13. `env` 来自外部输入。阅读下面的代码，判断是否存在 SSRF；如果存在，请追踪请求目标的数据流，并构造能够改变实际 Host 的输入。**

```go
func Check(id, env string) bool {
	host := "https://partner.example.com"
	if env != "" {
		host = fmt.Sprintf("https://partner.%s.example.com", env)
	}

	path := "/api/v2/shop/auth_partner"
	query := url.Values{}
	query.Set("partner_id", id)
	query.Set("redirect", "https://example.org")
	apiURL := host + path + "?" + query.Encode()

	client := &http.Client{}
	req, err := http.NewRequestWithContext(
		context.Background(), http.MethodGet, apiURL, http.NoBody,
	)
	if err != nil {
		return false
	}

	res, err := client.Do(req)
	if err != nil {
		return false
	}
	defer res.Body.Close()
	return res.StatusCode == http.StatusOK
}
```

**参考要点：** `env` 从外部输入直接进入 Host 拼接，然后经 `apiURL`、`NewRequestWithContext` 到达 `client.Do`。例如传入 `env=attacker.test/`，生成的 URL 以 `https://partner.attacker.test/.example.com/...` 开头，实际 Host 是 `partner.attacker.test`。`http.Client` 默认跟随重定向，且代码没有对重定向目标或解析后的 IP 重新校验，会进一步扩大可访问范围。

**追问：** 如果只限制 `env` 不能包含 `/`，是否足以建立可靠的 Host 安全边界？默认跟随重定向会如何影响 SSRF 的可访问范围？

**14. 阅读下面的 OAuth 登录代码，判断 `state` 校验是否有效，以及登录完成后的跳转能否离开本站。请给出可以验证结论的输入。**

```python
def oauth_start(request):
    state = secrets.token_urlsafe(32)
    next_url = request.GET.get("next", "/")
    request.session["oauth_state"] = state
    request.session["login_next"] = next_url

    response = redirect(build_authorize_url(
        client_id=settings.CLIENT_ID,
        redirect_uri=settings.CALLBACK_URL,
        state=state,
    ))
    return response

def oauth_callback(request):
    expected_state = request.session.pop("oauth_state", "")
    received_state = request.GET.get("state", "")
    if not expected_state or not hmac.compare_digest(
        expected_state, received_state
    ):
        return HttpResponseBadRequest("invalid state")

    code = request.GET.get("code")
    token = exchange_code_for_token(code, settings.CALLBACK_URL)
    login(request, fetch_user(token))

    next_url = request.session.pop("login_next", "/")
    if not next_url.startswith("/"):
        next_url = "/"
    return HttpResponseRedirect(next_url)
```

**参考要点：** `state` 使用高熵随机值、保存在服务端 Session 中、回调时进行相等比较并通过 `pop` 保证一次性使用；在给定代码范围内，这部分能够有效绑定登录发起与回调，不应误报 `state` 缺失。问题在于 `next_url.startswith("/")`：输入 `//attacker.example/path` 仍以 `/` 开头，但浏览器会把它解释为协议相对的跨站 URL，因此可以造成登录后的开放重定向。应使用标准 URL 解析器拒绝 scheme、host、反斜杠和其他非站内形式，或只允许预定义的站内路径映射。

**追问：** 该开放重定向是否会自动把当前站点的 Session Cookie 或 OAuth code 发送给攻击者？审计报告应如何准确描述其实际影响，避免夸大？

**15. `path` 完全由用户控制。输入 `; id` 或 `$(id)` 是否会被执行？即使不会，这段实现还存在哪些风险？**

```java
@GetMapping("/files")
public Process listFiles(@RequestParam String path) throws IOException {
    return Runtime.getRuntime().exec(
        new String[]{"/bin/ls", "-l", path}
    );
}
```


**参考要点：** `Runtime.exec(String[])` 不经过 Shell，这些输入通常只是一个参数，不会触发 Shell 语法。仍需考虑以 `-` 开头的选项注入、任意路径读取、权限边界和进程资源消耗。优先使用语言的文件 API；若必须调用程序，使用 `--` 终止选项并对白名单路径做授权。

**16. `env` 和 `region` 来自请求参数。阅读下面的代码，判断是否存在 SSRF；如果存在，请追踪请求目标的数据流，构造能够改变实际 Host 的输入，并指出受影响的分支。**

```go
func CheckService(ctx context.Context, env, region string) error {
	endpoint := GetDomainByEnv(env, region)
	req, err := http.NewRequestWithContext(
		ctx, http.MethodGet, endpoint+"/health", http.NoBody,
	)
	if err != nil {
		return err
	}

	resp, err := http.DefaultClient.Do(req)
	if resp != nil {
		defer resp.Body.Close()
	}
	return err
}

func GetDomainByEnv(env string, region string) string {
	regionStr := strings.ToLower(region)

	if env == setting.ENV_DEV {
		env = setting.ENV_TEST
	} else if env == setting.ENV_STABLE {
		env = "test-stable"
	}

	switch regionStr {
	case "id":
		if env == setting.ENV_LIVE {
			return "https://gateway.id.example.com"
		}
		return fmt.Sprintf("https://gateway.%s.id.example.com", env)

	case "sg":
		if env == setting.ENV_LIVE {
			return "https://gateway.sg.example.com"
		}
		return fmt.Sprintf("https://gateway.%s.sg.example.com", env)
	}

	return "https://gateway.sandbox.id.example.com"
}
```

**参考要点：** `env` 和 `region` 来自请求参数，返回的 `endpoint` 经 `NewRequestWithContext` 到达 `http.DefaultClient.Do`。`region` 经过 `switch` 枚举，未知值会进入固定默认地址；主要问题是未知 `env` 没有被拒绝，并在 `id`、`sg` 的非 Live 分支中直接进入 Host 拼接。例如 `env=attacker.test/`、`region=id` 会生成 `https://gateway.attacker.test/.id.example.com/health`，实际 Host 是 `gateway.attacker.test`。两个 Live 分支和默认分支返回固定地址，不受该输入影响。修复关键是在拼接前用完全枚举拒绝未知 `env`，而不是只转换少数特殊值。

**追问：** 为什么 `region` 的当前处理方式和 `env` 不同？如果只过滤 `/`，是否足以建立可靠的 Host 安全边界？

**17. 逐个判断 `tenant_id`、`status`、`sort` 和 `direction` 能否改变 SQL 结构，并说明修复方案。**

```python
@app.get("/api/orders")
def list_orders():
    tenant_id = current_user.tenant_id
    status = request.args.get("status", "paid")
    sort = request.args.get("sort", "created_at")
    direction = request.args.get("direction", "desc")

    sql = text(f"""
        SELECT id, amount, status, created_at
        FROM orders
        WHERE tenant_id = :tenant_id AND status = :status
        ORDER BY {sort} {direction}
        LIMIT 100
    """)
    return db.session.execute(sql, {
        "tenant_id": tenant_id,
        "status": status,
    })
```


**参考要点：** `tenant_id` 和 `status` 使用绑定参数，通常不能改变 SQL 结构；`sort` 和 `direction` 通过字符串插值进入结构，存在注入风险。应把允许值映射到 ORM 列对象及 `asc()`、`desc()`，并确认租户上下文来自可信身份。

**18. 根据下面的业务规则，判断代码是否完整实现了授权要求；如果没有，攻击者需要控制什么，服务端缺少什么校验？**

业务规则：普通用户只能读取自己创建的报告；`TENANT_ADMIN` 可以读取本租户的全部报告；任何人都不能跨租户读取。

```java
@GetMapping("/api/reports/{reportId}")
@PreAuthorize("isAuthenticated()")
public ReportDto getReport(
        @PathVariable long reportId,
        @AuthenticationPrincipal LoginUser currentUser) {

    Report report = reportRepository.findById(reportId)
        .orElseThrow(NotFoundException::new);

    if (!Objects.equals(
            report.getTenantId(), currentUser.getTenantId())) {
        throw new AccessDeniedException("forbidden");
    }
    return reportMapper.toDto(report);
}
```


**参考要点：** 代码只实现租户隔离，没有执行“普通用户只能读取本人报告”的对象级授权。普通用户改变路径中的 `reportId`，即可尝试读取同租户其他用户的报告。应明确实现“本人所有”或“同租户管理员”两个条件，最好在仓储查询中加入授权约束。

**追问：** UUID 或前端隐藏 ID 为什么不能替代授权？应如何覆盖授权回归测试？

**19. `html/template` 默认会转义输出。这段代码是否仍有安全问题？如果产品确实需要富文本，应如何处理？**

普通用户可以编辑个人简介，内容存入数据库。其他用户访问个人页时执行以下代码：

```go
var page = template.Must(template.New("profile").Parse(`
<h1>{{.Name}}</h1>
<div class="bio">{{.Bio}}</div>`))

type ProfileView struct {
	Name string
	Bio  template.HTML
}

func ShowProfile(w http.ResponseWriter, user User) error {
	return page.Execute(w, ProfileView{
		Name: user.Name,
		Bio:  template.HTML(user.Bio),
	})
}
```


**参考要点：** `user.Bio` 是持久化的不可信输入，却被转换成 `template.HTML`；该类型声明内容已经可信，会绕过自动转义，形成存储型 XSS。纯文本应保持 `string`；富文本使用成熟 sanitizer，按标签、属性和 URL 协议白名单清洗，并保证服务端与前端处理一致。CSP 只是纵深防御。

**20. 请求 `/tracking?no=abc` 后，参数 `no` 会进入下面代码生成的 `value` 属性。代码只删除了尖括号。判断是否仍然存在 XSS，并构造一个不包含 `<`、`>` 的验证输入，说明它能够生效还取决于哪些浏览器安全控制。**

```python
@app.get("/tracking")
def tracking():
    no = request.args.get("no", "")
    no = no.replace("<", "").replace(">", "")

    return f'''<form action="https://example.com/tracking" method="post">
      <input type="text" id="search-tn-input"
             required="required" value="{no}">
    </form>'''
```


**参考要点：** 输入位于双引号包围的 HTML 属性上下文，只过滤尖括号并不能阻止攻击者使用 `"` 结束 `value` 属性，再注入新的事件属性。例如可使用 `" autofocus onfocus=alert(1) x="` 验证属性逃逸；它不需要新的标签。是否执行还取决于页面是否允许获得焦点、浏览器行为以及 CSP 是否禁止内联事件处理器。修复应针对 HTML 属性上下文进行正确编码，使 `"` 变成 `&quot;` 等文本，而不是继续扩充字符黑名单；模板引擎的自动转义不能被关闭或绕过。

## 四、场景题

**21. Linux 服务器疑似被入侵，你会如何排查和处置？**


**参考要点：** 确认影响与授权，记录时间线，采集进程、网络、登录、账户、计划任务、服务和内存等易失证据；结合业务影响决定隔离；从入口、持久化、横向和数据影响定界；轮换凭据、修复根因并从可信环境恢复。未保留证据就重启、清日志或只运行杀毒工具是明显风险。

**22. 如果让你测试一个系统的登录模块，你会如何进行黑盒测试和代码审计？**


**参考要点：** 身份枚举、爆破与速率限制、验证码生命周期、会话固定、MFA 绕过、OAuth state/redirect、找回密码和跨租户授权。较好的回答会先画身份状态机，再用黑盒结果定位代码分支并形成回归用例。

**追问：** 如果登录接口限制了验证码和失败次数，但注册、找回密码、短信登录等接口没有相同限制，你会如何系统评估整条认证链路？

**23. 对下面四项扫描结果进行风险排序，并说明推动修复前还需要收集哪些证据。**

扫描产生以下结果：

1. 公网管理接口疑似存在反序列化 RCE，需要登录，已有公开利用代码。
2. 公网个人资料存在存储型 XSS，普通用户可以触发。
3. 仅 VPN 可达的财务系统疑似 SQL 注入。
4. 已停止使用的测试环境组件存在 CVSS 9.8 漏洞，但资产归属不清。


**参考要点：** 综合可利用性证据、暴露、前置身份、资产价值、攻击链、现有控制和公开利用活跃度。通常先确认有公开利用代码的公网 RCE；同时处理可稳定复现的存储型 XSS；财务 SQL 注入虽仅 VPN 可达但资产价值高，不能机械降级；测试环境先确认是否真的下线并隔离。不要求唯一排序，但必须说明假设。

**24. 一个影响公司 Java 网关的高危漏洞刚刚公开，目前没有正式补丁，资产清单也不完整。请说明第一个工作日内的响应计划，包括资产确认、临时缓解、监控和业务沟通。**


**参考要点：** 多源补齐资产，识别版本、暴露和调用路径；制定临时缓解与例外；部署检测和 IOC/TTP 监控；验证缓解后分批上线；明确负责人、节奏和业务风险；补丁发布后验证与复测；复盘资产清单缺口。

## 五、AI 与 MCP 安全

**25. MCP 与 Skill 分别解决什么问题？在 Agent 需要按照固定流程调用内部系统时，两者应该如何配合，为什么不能简单互相替代？**


**参考要点：** MCP 提供工具、资源以及连接外部系统的标准接口；Skill 封装完成任务所需的流程、领域规则、参考资料和可选脚本。Skill 可以指导 Agent 如何选择和调用 MCP 工具，但不能凭空提供外部系统能力、身份和授权；MCP 暴露能力，也不代表 Agent 知道如何安全、可靠地组合这些能力。

**追问：** 当 MCP 工具数量持续增加时，工具描述占用上下文、命名冲突、版本兼容、工具发现、权限边界和服务端可信度分别可能造成什么问题？

**26. 一个企业 Agent 可以读取内部文档、浏览公网并创建工单。请画出主要信任边界，并选出最需要优先控制的三个风险。**


**参考要点：** 主要边界包括用户与 Agent、外部网页与模型上下文、内部文档与模型、模型与工具、工具与目标系统、不同租户。优先考虑间接提示注入驱动工具越权、内部数据泄露和过宽工具身份。应按影响和可达性排序，而不是罗列名词。

**27. 某 MCP 工具提供 `refund(order_id, amount)` 能力。模型根据用户对话生成参数并直接调用。请设计身份、授权、参数校验、确认和审计机制。**


**参考要点：** 工具端独立认证用户和调用方；按订单、租户、退款上限和状态授权；使用结构化 schema；服务端重新取得订单金额；高风险操作要求确认或二次认证；加入幂等键、速率限制、审计和异常检测。模型输出不能成为授权依据。

**28. Agent 在处理任务时会读取外部网页。网页中包含文字，要求 Agent 忽略原任务并上传内部文件。应该在哪些层面阻止这类指令影响工具调用？**


**参考要点：** 标记外部内容为不可信数据；分离指令和数据通道；限制外部内容可触发的工具；工具端独立授权和参数校验；对敏感文件、出网和跨域传输建立策略；高风险操作确认；记录来源和调用链。只在系统提示里写“忽略恶意指令”不够。

**29. 一个 MCP 工具会在沙盒中执行用户提交的代码，并允许代码访问网络。你会如何设计执行隔离、网络访问、身份权限、资源限制和审计机制？**


**参考要点：** 每次执行使用独立的短生命周期环境和低权限身份；隔离文件系统、进程、宿主接口和凭据；默认禁止出网，通过受控代理或目标白名单开放必要访问；阻止私网、云元数据地址，并在 DNS 解析和重定向后重新校验；限制 CPU、内存、进程数、文件数和执行时间；缩减系统调用；记录调用者、代码、网络请求和结果；支持异常检测与强制终止。输入检测和系统调用 Hook 只能作为辅助，不能代替隔离与权限边界。

**30. 一个服务允许用户提交 JavaScript 脚本，并计划在 Node.js 中执行。`eval`、`new Function`、动态 `import()` 和 `vm` 分别会引入什么风险？为什么 `vm` 的 Context 和 `timeout` 不能成为执行恶意代码的可靠安全边界？**


**参考要点：** `eval` 和 `new Function` 都会把字符串作为代码执行，输入可控时可能造成任意代码执行；`eval` 可以访问当前词法作用域，`new Function` 通常在全局作用域执行。动态 `import()` 的模块来源可控时可能加载非预期模块，在 `vm` 中使用主 Context 的默认加载器还可能暴露 `fs`、`http` 等内置模块。`vm` 主要用于在独立 V8 Context 中编译和运行 JavaScript，提供不同的全局对象和变量环境，但不提供操作系统级隔离；代码仍与宿主共享进程、身份和资源，传入的宿主对象也可能暴露敏感能力。`timeout` 主要限制一次执行，异步任务和宿主回调仍可能绕过其控制。执行恶意代码需要独立进程、容器或虚拟机，并限制文件系统、网络、系统调用、凭据、CPU、内存和执行时间。

**追问：** 即使没有向 Context 暴露 `process` 或 `require`，恶意代码还可能如何消耗资源或通过宿主对象影响服务？

**31. 一个修复 Agent 根据扫描告警修改代码并提交合并请求。你如何验证它消除了漏洞、没有破坏正常功能，也没有引入新的权限或供应链风险？**


**参考要点：** 人工确认根因；用原始 PoC 做安全回归；增加正常、边界和相邻绕过测试；审查生成代码、依赖和权限变化；限制 Agent 仓库及合并权限；分批上线并监控。需要区分“告警消失”和“根因被修复”。

## 六、工程能力

**32. 一个常用 npm 依赖发布了新版本，新版本新增了安装脚本并开始访问外部网络。你会如何判断它是否遭到供应链投毒，并控制已经使用该版本的风险？**


**参考要点：** 比较版本代码、依赖树、安装脚本、维护者和发布流程变化；检查混淆代码、锁文件、制品来源及完整性；在无凭据的隔离环境观察进程、文件和网络行为；确认受影响的仓库、构建任务和制品；阻断或回退可疑版本及网络访问；排查泄露迹象并轮换可能暴露的凭据；补充版本固定、依赖审批、最小权限 CI、制品校验和持续监控。静态与动态分析只是调查手段，需要落到影响确认、遏制和恢复。

**33. 设计一个会主动向生产接口发请求的安全扫描器。你会如何控制误报、漏报以及对业务的影响？**


**参考要点：** 分层探测与验证、证据等级、速率和并发限制、幂等请求、安全窗口、资产授权、去重、停止条件、可观测性、人工复核和紧急关闭机制。

## 七、补充安全场景

**34. 一个文件上传服务先将用户提交的文件保存到 Web 可访问目录，再异步检查文件类型并删除不合格文件。这个流程存在哪些安全问题，你会如何重新设计上传、校验、存储和访问链路？**


**参考要点：** 未完成校验的文件已经可以被访问，攻击者可能利用校验与删除之间的时间窗口；还需检查客户端文件名、路径穿越、扩展名与内容不一致、不同解析器判断差异、可执行权限、覆盖已有文件、压缩包或大文件资源消耗。较好的方案会先写入不可执行且不可公开访问的隔离区，使用服务端生成的文件名，完成大小、格式和内容校验后再发布，并通过独立域名、固定响应头和最小权限控制访问。

**35. 一个找回密码流程先验证手机号和短信验证码，再由另一个接口根据客户端提交的 `user_id` 设置新密码。你会如何验证是否能够重置其他用户的密码，并如何重新设计这个流程？**


**参考要点：** 尝试替换 `user_id`、手机号、流程步骤和响应状态，验证能否跳过前置步骤、复用验证码或并发提交。服务端应在验证码通过后签发与用户、用途和当前流程绑定的短期一次性凭证，修改密码接口只能使用该凭证中确定的用户身份，不能重新信任客户端提交的身份字段；成功后还应使现有会话或高风险令牌失效并记录审计事件。

**36. 一个转账、退款或优惠券领取接口在并发请求下可能被重复执行。你会如何验证是否存在竞态条件，并从业务流程和数据库两层防止重复执行？**


**参考要点：** 使用同一身份、业务对象和请求参数并发提交，观察余额、库存、状态和流水是否出现不一致，不能只依据 HTTP 响应判断。修复应包括明确的状态迁移、幂等键、数据库唯一约束或条件更新，并根据一致性要求选择事务、行锁或乐观锁。分布式锁只能作为辅助，还要考虑锁失效、重试、消息重复消费和失败后的对账补偿。

**37. 阅读下面的 Java 代码，判断攻击者能否通过 `data` 触发反序列化漏洞，并说明从外部输入到危险操作的数据流、实际利用条件、影响和修复方案。**

```java
record ImportRequest(String data) {}

@PostMapping("/api/profile/import")
public String importProfile(@RequestBody ImportRequest request)
        throws Exception {
    byte[] bytes = Base64.getDecoder().decode(request.data());
    try (ObjectInputStream input = new ObjectInputStream(
            new ByteArrayInputStream(bytes))) {
        Object profile = input.readObject();
        return profile.toString();
    }
}
```


**参考要点：** `data` 来自请求体，经 Base64 解码后未经可信性校验直接进入 `ObjectInputStream.readObject()`，因此应用会对攻击者控制的原生 Java 序列化数据执行反序列化。是否能够直接实现代码执行取决于类路径中是否存在可用 Gadget、JDK 和依赖版本以及运行权限，但拒绝服务、非预期对象构造等风险不应被忽略。优先使用明确的数据格式和固定 DTO；确实无法移除原生反序列化时，使用严格的 `ObjectInputFilter`、类型和复杂度限制，并同时缩减依赖与进程权限。

**追问：** 仅把反序列化后的对象强制转换成 `Profile`，为什么通常不能阻止恶意对象在转换前触发副作用？

**38. 阅读下面的 XML 导入代码，判断是否存在 XXE 风险；说明结论中哪些可以直接从代码确定，哪些取决于 JAXP 实现或运行配置，并给出验证与修复方法。**

```java
@PostMapping(
    value = "/api/orders/import",
    consumes = "application/xml"
)
public String importOrders(@RequestBody byte[] body) throws Exception {
    DocumentBuilderFactory factory =
        DocumentBuilderFactory.newInstance();
    DocumentBuilder builder = factory.newDocumentBuilder();
    Document document = builder.parse(
        new ByteArrayInputStream(body));
    return document.getDocumentElement().getTextContent();
}
```


**参考要点：** 请求体完全可控并直接交给未显式加固的 XML 解析器，这是可以从代码确认的风险数据流。是否能够读取本地文件、访问内网或通过外部 DNS/HTTP 带出数据，还取决于具体解析器、JDK 安全属性、网络和文件权限，需用无破坏性的外部实体及受控回连验证。修复应显式禁止 DOCTYPE、外部通用实体、外部参数实体和外部 DTD，限制外部 Schema 与 Stylesheet 访问，并用回归测试确认实际使用的解析器配置。

**39. 一个文件下载接口存在任意文件读取漏洞，攻击者能够控制文件路径并读取服务进程有权限访问的文件。除了读取常见的系统账户文件，你会如何扩大攻击面，并按什么顺序验证可能造成的进一步影响？**


**参考要点：** 先判断运行用户、部署方式、文件系统边界和可读取范围，再寻找应用配置、环境变量暴露路径、源码、日志、数据库连接信息、云凭据、SSH 密钥、令牌签名密钥和容器挂载信息。随后验证所得信息能否访问数据库、对象存储、内部接口或其他租户数据，以及能否结合日志污染、可控文件或其他漏洞形成攻击链。回答应区分已经确认的读取能力、需要环境条件的扩展路径和验证行为可能造成的业务风险。

**40. 客户发现服务器上存在 WebShell，但不知道它何时被写入、通过哪个入口上传。你会如何保护现场、构建时间线并定位最初的漏洞入口？**


**参考要点：** 先保存文件元数据、样本、进程、网络连接和相关日志等证据，再围绕文件时间、首次访问和异常进程建立时间线；从 Web 访问日志中向前追踪上传、写文件或命令执行请求，并结合应用日志、中间件错误日志、文件审计、Shell 历史和安全设备记录交叉验证。需要区分落地入口、后续控制流量和攻击者利用 WebShell 继续执行的操作，不能发现文件后直接删除并结束调查。

**41. 多台主机先后出现异常，部分安全日志已经被清理。你会如何判断最初入口、主机之间的横向移动关系以及当前受影响范围？**


**参考要点：** 以统一时间线关联登录、进程创建、服务、计划任务、远程管理、网络流量、身份认证和业务服务日志；从证据相对完整的主机向前、向后寻找共同账号、源地址、工具和行为模式。日志缺失本身也是证据，但不能据此直接断言攻击路径；应使用终端服务日志、EDR、域控、VPN、防火墙、DNS、代理及其他主机记录补充，并持续扩大 IOC 和 TTP 排查范围。

**42. 一个 Java Web 服务持续出现对不存在路径的请求并返回 `200`，磁盘上也找不到对应文件。你会如何判断是否存在内存马，并在尽量保留证据的前提下完成处置？**


**参考要点：** 先排除正常的统一路由、错误页和代理行为，再结合访问日志、响应特征、进程网络连接和类加载情况判断。Java 环境可检查异常 Servlet、Filter、Listener、Interceptor、Controller 映射及运行时被修改的类，必要时使用线程栈、堆、类转储或诊断工具保留证据。处置不能只删除磁盘文件或重启进程，还应确认注入入口、持久化方式、受影响凭据和横向活动，并从可信制品恢复。

**43. 接手一个陌生的 Java 应用，只有源码、构建文件和部署配置，你会如何在有限时间内确定代码审计顺序，并避免只依赖组件版本扫描？**


**参考要点：** 先识别框架、入口、路由、过滤器、鉴权模型、数据访问方式、文件和网络能力，再检查构建依赖、配置、密钥和外部暴露；结合 Source 到 Sink 与 Sink 反向追踪审计 SQL、命令、文件、反序列化、XML、SSRF 和模板输出。组件版本扫描只能提供候选风险，还需确认依赖是否实际加载、危险功能是否可达、外部输入和权限条件；业务侧重点包括越权、找回密码、支付和多步骤状态校验。

**44. 安全设备产生了一条疑似攻击告警，但仅凭请求中的 Payload 无法判断是否利用成功。你会收集哪些证据，并如何区分扫描、利用尝试和已经造成影响的攻击？**


**参考要点：** 关联请求与响应、状态码、响应长度和延迟，并检查应用、中间件、主机、身份、数据库、DNS、代理和出网日志；寻找异常进程、文件变化、子进程、回连、权限变化和后续操作。扫描通常缺少针对性和成功后的连续行为，利用尝试具备匹配的入口但未必满足环境条件，确认成功需要服务端副作用或独立证据。较好的回答会说明证据可信度、时间关联误差和无法确认时的保守处置方式。

## 八、45 分钟推荐组合

### 应用安全 / 代码审计

1. 项目责任边界：7 分钟
2. XSS 或 SSRF 基础：5 分钟
3. 告警列表 SQL 审计：8 分钟
4. 从合作方检查、订单查询、报告查询、个人简介中选一道代码题：12 分钟
5. 登录链路测试：8 分钟
6. 候选人提问：5 分钟

### 安全运营 / 应急响应

1. 项目责任边界：7 分钟
2. SQL 注入或 SSRF 基础：5 分钟
3. Linux 主机异常响应：12 分钟
4. 漏洞排序与闭环：8 分钟
5. 重大漏洞响应：8 分钟
6. 候选人提问：5 分钟

### AI / MCP 安全

1. 项目责任边界：7 分钟
2. SSRF 基础：5 分钟
3. Agent 威胁建模：10 分钟
4. MCP 工具调用授权：10 分钟
5. 外部内容与间接提示注入：8 分钟
6. 候选人提问：5 分钟

## 九、结论记录

- **最强证据：**
- **主要风险：**
- **达到岗位最低要求：** 是 / 否
- **仍需验证：**
- **建议：** 强烈推荐 / 推荐 / 保留 / 不推荐

最终结论至少引用两项独立证据，其中一项应来自代码题或场景题。
