# AGENTS.md

## 项目目标

本项目用于整理和维护个人安全面试题库。优先保留来源于个人真实项目、真实故障和真实面试经历，且能够验证候选人实际能力的题目。

## 维护范围

- 题库只维护在项目根目录的 `精选面试题库.md`。
- 不创建候选人版、面试官版或其他内容重复的题库文件。
- 不创建内容重复的题库文件。

## 内容组织

- 题目使用连续序号和普通正文格式 `**序号. 问题**`，不要使用 Markdown 标题。
- 每道题都应写成完整、自然、可直接向候选人提出的问题，避免使用标签式短语代替题目。
- 不添加开场说明、使用方法等非题目内容；题目后可附参考要点和必要追问，但不要拆成零散小题。
- 调整题目后检查并更新全部序号。

## 题目筛选标准

- 每道题必须有明确的考察目的，以及判断候选人回答质量的方法。
- 删除只能考察概念背诵、答案过于明显、缺乏区分度或无法说明考察意义的题目。
- 删除与目标考点无关的背景、业务描述、代码分支和干扰信息。
- 题干应简洁，但不能为了简短而丢失分析所需的上下文。
- 修改已有题目时保留有价值的约束和分析深度，避免过度简化。
- 能保留核心考点时改写；无法形成有效评价标准时删除，不为凑数量而保留。
- 避免多道题只更换变量名、编程语言或业务背景，实质考察相同内容。

## 安全代码审计题

- 代码审计题主要考察安全分析能力，不要退化成普通开发、语法或代码风格题。
- 题目必须提供可以实际审计的具体代码，不能只描述“某段代码存在问题”。
- 代码应包含分析漏洞所需的输入来源、调用链、安全边界和关键处理逻辑。
- 候选人应能够根据代码识别漏洞、说明利用条件、分析影响并给出修复方案。
- 参考要点必须区分确定存在的问题和依赖外部条件的问题，不把推测写成确定漏洞。
- 删除与漏洞成因无关的签名、业务字段和辅助逻辑。
- 控制代码长度和分支数量，只保留理解漏洞所需的逻辑。
- 示例域名统一使用 `example.com`、`example.org` 等保留域名，不出现真实公司或内部域名。
- SSRF 题重点考察 URL 解析、协议限制、重定向、DNS、内网地址判断和请求库行为，不混入与 SSRF 无关的 HMAC 签名逻辑。

## 修改流程

1. 修改前先阅读 `精选面试题库.md` 中相关题目及其上下文。
2. 判断问题属于题干冗余、考点不清、代码不足、难度失衡、内容重复还是题目本身价值不足。
3. 根据题目筛选标准决定改写或删除。
4. 修改后检查序号是否连续、Markdown 结构是否完整、代码围栏是否成对，以及题目之间是否重复。
5. 向用户简要说明具体修改内容；用户只要求直接调整时，不附加冗长分析。
